# macOS
.DS_Store

# Config files generated by cert.sh / cert.ps1
cert.conf
cert.windows.conf
domains.conf

# Domain folders auto-discovered/created by cert.sh next to the script
# (e.g. example.com/, sub.example.co.uk/) — anything containing a dot
/*.*/
